export enum Role {
  EMPLOYEE = 'employee',
  MANAGER = 'manager',
  HR_ADMIN = 'hr_admin',
  FINANCE_ADMIN = 'finance_admin',
  SUPER_ADMIN = 'super_admin',
}

export enum TokenType {
  ACCESS = 'access',
  REFRESH = 'refresh',
  ACTIVATION = 'activation',
  RESET = 'reset',
}
